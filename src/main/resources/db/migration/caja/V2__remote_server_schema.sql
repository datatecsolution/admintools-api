-- V2: Tablas nuevas para cajas (si aplica).
-- Los ALTERs condicionales están en V3 (Java) porque Flyway SQL
-- no soporta DELIMITER ni procedimientos almacenados inline.

-- Actualmente las cajas no requieren tablas nuevas adicionales.
-- Esta migración se reserva como placeholder para mantener la
-- numeración alineada con common.
SELECT 1;
